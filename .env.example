PORT=3000

DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=todo_keep

# Password for the /admin panel (board management: view all boards, delete,
# restore, and permanently purge). Leave blank to disable admin login entirely.
ADMIN_PASSWORD=
